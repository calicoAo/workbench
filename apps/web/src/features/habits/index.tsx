import { useQuery, useQueryClient } from "@tanstack/react-query";
import { Activity, Check, ChevronRight, CircleSlash2, Plus, Target, X } from "lucide-react";
import { type FormEvent, type ReactNode, useEffect, useState } from "react";
import { createPortal } from "react-dom";
import { Link, useSearchParams } from "react-router";
import type { Request } from "../../app/api";
import { queryKeys } from "../../app/query";
import { Badge, Button, FilterChip, IconButton } from "../../shared/ui";

type Rule = { id: number; effectiveFrom: string; frequencyType: number; weekdayMask: number | null; weeklyTarget: number | null; enabled: number };
type Goal = { id: number; effectiveFrom: string; targetValue: string };
export type Habit = { id: number; name: string; description: string | null; categoryId: number | null; startDate: string; endDate: string | null; recordMode: number; unit: string | null; taskCompletionEnabled: number; version: number; archivedAt: string | null; rules: Rule[]; goals: Goal[] };
type Occurrence = { id: number; occurrenceDate: string; status: number; actualValue: string | null; skipReason: string | null; source: number; version: number };
type SummaryItem = Habit & { frequencyType: number; targetValue: number; weeklyTarget?: number; completed?: number; weekFrom?: string; weekTo?: string; status: "PENDING" | "MISSED" | "PARTIAL" | "COMPLETED" | "SKIPPED" | "IN_PROGRESS"; occurrence: Occurrence | null };
export type HabitSummary = { date: string; weekFrom: string; weekTo: string; items: SummaryItem[]; specialized: { water: { cups: number; targetCups: number; status: string }; morningWriting: { completed: boolean }; sleep: { recorded: boolean; durationMinutes?: number; qualityScore?: number | null } } };
type Detail = { habit: Omit<Habit, "rules" | "goals">; rules: Rule[]; goals: Goal[]; occurrences: Occurrence[]; links: Array<{ id: number; occurrenceDate: string; taskId: number; completeHabitOnTask: number }> };
type Category = { id: number; name: string };
const modeLabels = ["完成", "次数", "数量", "分钟"];
const frequencyLabels = ["每天", "指定星期", "每周目标"];
const weekdayLabels = ["一", "二", "三", "四", "五", "六", "日"];
const statusLabels: Record<string, string> = { PENDING: "待记录", MISSED: "未达成", PARTIAL: "部分达成", COMPLETED: "已达成", SKIPPED: "已跳过", IN_PROGRESS: "本周进行中" };

export function useHabitSummary(request: Request, userId: number, date: string) {
  return useQuery({ queryKey: queryKeys.habitSummary(userId, date), queryFn: () => request<HabitSummary>(`/api/habits/summary?date=${date}`) });
}

export function HabitTodaySnapshot({ request, userId, date }: { request: Request; userId: number; date: string }) {
  const query = useHabitSummary(request, userId, date);
  if (query.isPending) return <section className="habit-snapshot"><Activity size={17} /><span>习惯状态加载中</span></section>;
  const items = query.data?.items ?? [];
  const done = items.filter((item) => item.status === "COMPLETED").length;
  return <Link className="habit-snapshot" to={`/routines?date=${date}`}><Activity size={17} /><span><strong>今日习惯 {done}/{items.length}</strong><small>{items.length ? items.slice(0, 2).map((item) => item.name).join(" · ") : "今天没有应发生的通用习惯"}</small></span><ChevronRight size={16} /></Link>;
}

export function RoutinesFeature({ request, userId, date, timezone, categories, lifeContent, onError }: { request: Request; userId: number; date: string; timezone: string; categories: Category[]; lifeContent: ReactNode; onError: (message: string, title?: string) => void }) {
  const [params, setParams] = useSearchParams();
  const initialTab = params.get("habit") ? "habits" : (params.get("view") as "today" | "habits" | "life") || "today";
  const [tab, setTab] = useState(initialTab);
  const summary = useHabitSummary(request, userId, date);
  const habits = useQuery({ queryKey: queryKeys.habits(userId), queryFn: () => request<Habit[]>("/api/habits") });
  const [creating, setCreating] = useState(false);
  const selectedId = Number(params.get("habit") || 0);
  useEffect(() => { setTab(params.get("habit") ? "habits" : (params.get("view") as "today" | "habits" | "life") || "today"); }, [params]);
  function choose(next: "today" | "habits" | "life") { setTab(next); const copy = new URLSearchParams(params); copy.set("view", next); if (next !== "habits") copy.delete("habit"); setParams(copy); }
  async function changed() { await Promise.all([habits.refetch(), summary.refetch()]); }
  return <section className="routines-route"><header className="routines-head"><div><p className="route-eyebrow">生活节奏</p><h1>习惯与生活记录</h1><p>按当时生效的规则记录，不改写过去。</p></div>{tab === "habits" ? <Button variant="primary" onClick={() => setCreating(true)}><Plus size={16} />新建习惯</Button> : null}</header><nav className="routines-tabs" aria-label="生活节奏视图"><FilterChip active={tab === "today"} onClick={() => choose("today")}>今日</FilterChip><FilterChip active={tab === "habits"} onClick={() => choose("habits")}>习惯</FilterChip><FilterChip active={tab === "life"} onClick={() => choose("life")}>生活记录</FilterChip></nav>{tab === "today" ? <HabitTodayView summary={summary.data} loading={summary.isPending} request={request} userId={userId} date={date} timezone={timezone} onChanged={changed} onError={onError} /> : null}{tab === "habits" ? selectedId ? <HabitDetail request={request} userId={userId} habitId={selectedId} date={date} timezone={timezone} onBack={() => { const copy = new URLSearchParams(params); copy.delete("habit"); setParams(copy); }} onChanged={changed} onError={onError} /> : <HabitLibrary habits={habits.data ?? []} loading={habits.isPending} date={date} onSelect={(id) => { const copy = new URLSearchParams(params); copy.set("habit", String(id)); setParams(copy); }} /> : null}{tab === "life" ? <div className="routines-life"><div className="routines-grid">{lifeContent}</div></div> : null}{creating ? <HabitCreateDialog request={request} date={date} categories={categories} onClose={() => setCreating(false)} onSaved={async () => { setCreating(false); await changed(); }} onError={onError} /> : null}</section>;
}

function HabitTodayView({ summary, loading, request, userId, date, timezone, onChanged, onError }: { summary?: HabitSummary; loading: boolean; request: Request; userId: number; date: string; timezone: string; onChanged: () => Promise<void>; onError: (message: string, title?: string) => void }) {
  if (loading) return <div className="route-state">正在加载今日习惯...</div>;
  const items = summary?.items ?? [];
  return <div className="habit-today-grid"><section className="habit-today-list"><header><div><p className="route-eyebrow">{date}</p><h2>今日习惯</h2></div><Badge>{items.length} 项</Badge></header>{items.length ? items.map((item) => <HabitTodayCard key={item.id} item={item} request={request} userId={userId} date={date} timezone={timezone} onChanged={onChanged} onError={onError} />) : <div className="habit-empty"><Check size={22} /><strong>今天没有应发生的习惯</strong><span>每周目标仍会在所属周内显示。</span></div>}</section><section className="specialized-summary"><header><p className="route-eyebrow">专用来源</p><h2>生活快照</h2></header><div><span><strong>饮水</strong><small>{summary?.specialized.water.cups ?? 0}/{summary?.specialized.water.targetCups ?? 8} 杯</small></span><span><strong>晨写</strong><small>{summary?.specialized.morningWriting.completed ? "已保存正文" : "尚未完成"}</small></span><span><strong>睡眠</strong><small>{summary?.specialized.sleep.recorded ? `${summary.specialized.sleep.durationMinutes} 分钟` : "尚未记录"}</small></span></div><Link to={`/routines?date=${date}&view=life`}>打开生活记录</Link></section></div>;
}

function HabitTodayCard({ item, request, userId, date, timezone, onChanged, onError }: { item: SummaryItem; request: Request; userId: number; date: string; timezone: string; onChanged: () => Promise<void>; onError: (message: string, title?: string) => void }) {
  const [value, setValue] = useState(item.occurrence?.actualValue ?? "");
  const [pending, setPending] = useState(false);
  async function record(skipped = false) { setPending(true); try { await request(`/api/habits/${item.id}/occurrences`, { method: "POST", body: JSON.stringify({ operationId: crypto.randomUUID(), occurrenceDate: date, recordTimezone: timezone, expectedVersion: item.occurrence?.version ?? 0, actualValue: item.recordMode === 0 ? undefined : Number(value || 0), skipped, skipReason: skipped ? "主动跳过" : undefined }) }); await onChanged(); } catch (error) { onError(message(error), "习惯记录没有保存"); } finally { setPending(false); } }
  async function task() { setPending(true); try { await request(`/api/habits/${item.id}/task`, { method: "POST", body: JSON.stringify({ operationId: crypto.randomUUID(), occurrenceDate: date, recordTimezone: timezone }) }); await onChanged(); } catch (error) { onError(message(error), "没有生成今日悬赏"); } finally { setPending(false); } }
  const weekly = item.frequencyType === 2;
  return <article className="habit-today-card"><div className="habit-card-copy"><span className={`habit-status is-${item.status.toLowerCase()}`}>{statusLabels[item.status]}</span><strong>{item.name}</strong><small>{weekly ? `本周 ${item.completed ?? 0}/${item.weeklyTarget ?? 1} 次` : `${frequencyLabels[item.frequencyType]} · 目标 ${item.targetValue}${item.unit ? ` ${item.unit}` : ""}`}</small></div>{weekly ? <div className="habit-week-progress" aria-label={`${item.name}周进度`}><i style={{ width: `${Math.min(100, ((item.completed ?? 0) / (item.weeklyTarget ?? 1)) * 100)}%` }} /></div> : null}<div className="habit-card-actions">{item.recordMode !== 0 ? <input aria-label={`${item.name}实际值`} className="field habit-value" min="0" step="any" type="number" value={value} onChange={(event) => setValue(event.target.value)} /> : null}<Button variant="primary" loading={pending} disabled={item.status === "COMPLETED"} onClick={() => void record(false)}><Check size={15} />{item.recordMode === 0 ? "完成" : "记录"}</Button><IconButton label={`跳过${item.name}`} disabled={pending} onClick={() => void record(true)}><CircleSlash2 size={16} /></IconButton><Button size="sm" disabled={pending} onClick={() => void task()}>转悬赏</Button></div></article>;
}

function HabitLibrary({ habits, loading, date, onSelect }: { habits: Habit[]; loading: boolean; date: string; onSelect: (id: number) => void }) {
  const [showArchived, setShowArchived] = useState(false);
  const visible = habits.filter((item) => showArchived || !item.archivedAt);
  if (loading) return <div className="route-state">正在加载习惯...</div>;
  return <div className="habit-library"><div className="habit-library-tools"><span>{visible.length} 个习惯</span><label><input type="checkbox" checked={showArchived} onChange={(event) => setShowArchived(event.target.checked)} />显示已归档</label></div>{visible.map((habit) => { const rule = [...habit.rules].reverse().find((item) => item.effectiveFrom <= date) ?? habit.rules.at(-1); const goal = [...habit.goals].reverse().find((item) => item.effectiveFrom <= date) ?? habit.goals.at(-1); return <button key={habit.id} className="habit-library-row" onClick={() => onSelect(habit.id)}><span className="habit-library-icon"><Target size={17} /></span><span><strong>{habit.name}</strong><small>{frequencyLabels[rule?.frequencyType ?? 0]} · {modeLabels[habit.recordMode]} · 目标 {Number(goal?.targetValue ?? 1)} {habit.unit ?? ""}</small></span>{habit.archivedAt ? <Badge>已归档</Badge> : <Badge tone={rule?.enabled ? "success" : "warning"}>{rule?.enabled ? "启用" : "停用"}</Badge>}<ChevronRight size={16} /></button>; })}{!visible.length ? <div className="habit-empty"><Target size={22} /><strong>还没有习惯</strong><span>从一个可持续的小目标开始。</span></div> : null}</div>;
}

function HabitDetail({ request, userId, habitId, date, timezone, onBack, onChanged, onError }: { request: Request; userId: number; habitId: number; date: string; timezone: string; onBack: () => void; onChanged: () => Promise<void>; onError: (message: string, title?: string) => void }) {
  const queryClient = useQueryClient();
  const query = useQuery({ queryKey: queryKeys.habit(userId, habitId), queryFn: () => request<Detail>(`/api/habits/${habitId}`) });
  const [effective, setEffective] = useState(date);
  const [target, setTarget] = useState("");
  const [frequency, setFrequency] = useState("0");
  const [weekdayMask, setWeekdayMask] = useState(31);
  const [weekly, setWeekly] = useState("3");
  async function command(path: string, body: Record<string, unknown>) { if (!query.data) return; try { await request(`/api/habits/${habitId}/${path}`, { method: "PUT", body: JSON.stringify({ operationId: crypto.randomUUID(), expectedVersion: query.data.habit.version, effectiveFrom: effective, ...body }) }); await queryClient.invalidateQueries({ queryKey: queryKeys.habit(userId, habitId) }); await onChanged(); } catch (error) { onError(message(error), "习惯没有更新"); } }
  if (query.isPending) return <div className="route-state">正在加载习惯详情...</div>;
  if (!query.data) return <div className="notes-error">习惯不存在</div>;
  const data = query.data;
  return <section className="habit-detail"><header><Button onClick={onBack}>返回习惯</Button><div><p className="route-eyebrow">习惯详情</p><h2>{data.habit.name}</h2><p>{data.habit.description || "没有说明"}</p></div><Badge tone={data.habit.archivedAt ? "neutral" : "success"}>{data.habit.archivedAt ? "已归档" : "进行中"}</Badge></header><div className="habit-detail-grid"><section><h3>历史记录</h3><div className="habit-history">{data.occurrences.map((item) => <div key={item.id}><span><strong>{item.occurrenceDate}</strong><small>{item.source === 1 ? "关联悬赏" : "手动记录"}</small></span><Badge tone={item.status === 1 ? "success" : item.status === 2 ? "neutral" : "warning"}>{["部分达成", "已达成", "已跳过"][item.status]}{item.actualValue ? ` · ${Number(item.actualValue)}` : ""}</Badge></div>)}{!data.occurrences.length ? <p>还没有实际记录。</p> : null}</div></section><aside><h3>规则与目标调整</h3><p className="habit-effective-note">新配置从所选日期起生效，之前的历史不会重新计算。</p><label>生效日期<input className="field" type="date" min={date} value={effective} onChange={(event) => setEffective(event.target.value)} /></label><div className="habit-edit-line"><input aria-label="新目标" className="field" type="number" min="0.01" step="any" placeholder="新目标" value={target} onChange={(event) => setTarget(event.target.value)} /><Button disabled={!target} onClick={() => void command("goal", { targetValue: Number(target) })}>更新目标</Button></div><div className="habit-edit-line"><select aria-label="新频率" className="field" value={frequency} onChange={(event) => setFrequency(event.target.value)}><option value="0">每天</option><option value="1">指定星期</option><option value="2">每周 N 次</option></select>{frequency === "2" ? <input aria-label="每周次数" className="field" type="number" min="1" max="7" value={weekly} onChange={(event) => setWeekly(event.target.value)} /> : null}<Button disabled={frequency === "1" && weekdayMask === 0} onClick={() => void command("rule", { rule: { frequencyType: Number(frequency), weekdayMask: frequency === "1" ? weekdayMask : null, weeklyTarget: frequency === "2" ? Number(weekly) : null } })}>更新频率</Button></div>{frequency === "1" ? <div className="weekday-picker" aria-label="调整星期">{weekdayLabels.map((label, index) => <FilterChip key={label} active={Boolean(weekdayMask & (1 << index))} onClick={() => setWeekdayMask(weekdayMask ^ (1 << index))}>周{label}</FilterChip>)}</div> : null}<div className="habit-state-actions"><Button onClick={() => void command("state", { action: "disable" })}>停用</Button><Button variant="danger" onClick={() => void command("state", { action: data.habit.archivedAt ? "restore" : "archive" })}>{data.habit.archivedAt ? "恢复" : "归档"}</Button></div><h3>版本记录</h3><ol className="habit-version-list">{data.rules.map((item) => <li key={`r${item.id}`}><strong>{item.effectiveFrom}</strong> 规则：{item.enabled ? frequencyLabels[item.frequencyType] : "停用"}</li>)}{data.goals.map((item) => <li key={`g${item.id}`}><strong>{item.effectiveFrom}</strong> 目标：{Number(item.targetValue)}</li>)}</ol></aside></div></section>;
}

function HabitCreateDialog({ request, date, categories, onClose, onSaved, onError }: { request: Request; date: string; categories: Category[]; onClose: () => void; onSaved: () => Promise<void>; onError: (message: string, title?: string) => void }) {
  const [draft, setDraft] = useState({ name: "", description: "", categoryId: "", startDate: date, endDate: "", recordMode: "0", unit: "", taskCompletionEnabled: false, frequencyType: "0", weekdayMask: 31, weeklyTarget: "3", targetValue: "1" });
  const [pending, setPending] = useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); setPending(true); try { await request("/api/habits", { method: "POST", body: JSON.stringify({ operationId: crypto.randomUUID(), name: draft.name, description: draft.description || null, categoryId: draft.categoryId ? Number(draft.categoryId) : null, startDate: draft.startDate, endDate: draft.endDate || null, recordMode: Number(draft.recordMode), unit: draft.unit || null, taskCompletionEnabled: Number(draft.recordMode) === 0 && draft.taskCompletionEnabled, rule: { frequencyType: Number(draft.frequencyType), weekdayMask: draft.frequencyType === "1" ? draft.weekdayMask : null, weeklyTarget: draft.frequencyType === "2" ? Number(draft.weeklyTarget) : null }, targetValue: Number(draft.targetValue) }) }); await onSaved(); } catch (error) { onError(message(error), "习惯没有创建"); } finally { setPending(false); } }
  return createPortal(<div className="modal-backdrop" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><form className="time-modal habit-create" onSubmit={submit}><header><div><p className="route-eyebrow">新的行为目标</p><h2>创建习惯</h2></div><IconButton label="关闭" type="button" onClick={onClose}><X size={17} /></IconButton></header><label>名称<input autoFocus required className="field" maxLength={120} value={draft.name} onChange={(event) => setDraft({ ...draft, name: event.target.value })} /></label><label>说明<textarea maxLength={4000} value={draft.description} onChange={(event) => setDraft({ ...draft, description: event.target.value })} /></label><div className="habit-form-grid"><label>开始日期<input required className="field" type="date" value={draft.startDate} onChange={(event) => setDraft({ ...draft, startDate: event.target.value })} /></label><label>结束日期（可选）<input className="field" type="date" min={draft.startDate} value={draft.endDate} onChange={(event) => setDraft({ ...draft, endDate: event.target.value })} /></label><label>类别（可选）<select className="field" value={draft.categoryId} onChange={(event) => setDraft({ ...draft, categoryId: event.target.value })}><option value="">不关联类别</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label>频率<select className="field" value={draft.frequencyType} onChange={(event) => setDraft({ ...draft, frequencyType: event.target.value })}><option value="0">每天</option><option value="1">指定星期</option><option value="2">每周 N 次</option></select></label></div>{draft.frequencyType === "1" ? <div className="weekday-picker" aria-label="选择星期">{weekdayLabels.map((label, index) => <FilterChip key={label} active={Boolean(draft.weekdayMask & (1 << index))} onClick={() => setDraft({ ...draft, weekdayMask: draft.weekdayMask ^ (1 << index) })}>周{label}</FilterChip>)}</div> : null}{draft.frequencyType === "2" ? <label>每周次数<input required className="field" type="number" min="1" max="7" value={draft.weeklyTarget} onChange={(event) => setDraft({ ...draft, weeklyTarget: event.target.value })} /></label> : null}<div className="habit-form-grid"><label>记录方式<select className="field" value={draft.recordMode} onChange={(event) => setDraft({ ...draft, recordMode: event.target.value, targetValue: event.target.value === "0" ? "1" : draft.targetValue })}>{modeLabels.map((label, index) => <option key={label} value={index}>{label}</option>)}</select></label><label>目标<input required className="field" disabled={draft.recordMode === "0"} type="number" min="0.01" step="any" value={draft.targetValue} onChange={(event) => setDraft({ ...draft, targetValue: event.target.value })} /></label><label>单位<input className="field" disabled={draft.recordMode === "0"} maxLength={32} value={draft.unit} onChange={(event) => setDraft({ ...draft, unit: event.target.value })} /></label></div>{draft.recordMode === "0" ? <label className="habit-checkbox"><input type="checkbox" checked={draft.taskCompletionEnabled} onChange={(event) => setDraft({ ...draft, taskCompletionEnabled: event.target.checked })} />关联悬赏完成时同步达成</label> : null}<footer><Button type="button" onClick={onClose}>取消</Button><Button variant="primary" loading={pending} type="submit">创建习惯</Button></footer></form></div>, document.body);
}

function message(error: unknown) { return error instanceof Error ? error.message : "操作失败"; }
