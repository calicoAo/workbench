{
  "name": "@workbench/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "tsc -p tsconfig.test.json --noEmit && tsx --test tests/*.test.ts",
    "test:workflows": "tsc -p tsconfig.test.json --noEmit && tsx --test tests/workflows.test.ts",
    "test:workflows:ci": "node --test tests/require-all-tests.test.mjs && tsc -p tsconfig.test.json --noEmit && tsx --test --test-reporter=spec --test-reporter-destination=stdout --test-reporter=./tests/require-all-tests.mjs --test-reporter-destination=stdout tests/workflows.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.8",
    "dotenv": "^16.4.7",
    "drizzle-orm": "^0.38.3",
    "hono": "^4.6.14",
    "mysql2": "^3.11.5",
    "pino": "^9.5.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
